# V4.1.0 測試清單

部署完成後，依序在 LINE 輸入：

1. `今天`
   - 應顯示台灣、美國、美元、三大新聞與 AI 市場重點。
2. `新聞`
   - 應顯示五則新聞與來源時間。
3. `台指`
   - 應正常顯示台指期，漲跌方向與數字一致。
4. `市場`
   - 應保留 V4.0.3 全球市場功能。
5. `0050`
   - 應正常顯示單一行情。
6. `停損50 10口 小台`
   - 應保留 Risk Manager 功能。

## OpenAI 選填測試

- Render 未設定 `OPENAI_API_KEY`：`今天` 仍應正常回覆本機規則摘要。
- Render 設定 `OPENAI_API_KEY`：重新部署後，`今天` 應顯示 AI 產生的 2～3 句摘要。
